# ── Servidor ──
PORT=3000
NODE_ENV=production

# ── Base de Datos (PostgreSQL) ──
DB_HOST=database
DB_PORT=5432
DB_USER=fmaputes_admin
DB_PASSWORD=CHANGE_ME
DB_NAME=fmaputes_db

# ── Autenticación ──
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# ── CORS ──
CORS_ORIGIN=*

# ── Python ──
PYTHON_SCRIPTS_PATH=/usr/src/app/scripts/python
